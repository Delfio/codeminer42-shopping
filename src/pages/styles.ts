import styled from 'styled-components';

export const ContainerForListing = styled.div`
    padding: 0 25px;
    margin-top: 25px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
`;
